package main

import (
	"fmt"
	"os"
	"strconv"
)

// Fizz Buzz
// Print all the numbers from 1 to N. If the number is evenly divisible by 3,
// then print "Fuzz" instead. If the number is evenly divisible by 5, then print
// "Buzz" instead. If the number is evenly divisible by 3 and 5, then print 
// "FizzBuzz" instead.

func main() {
	max, _ := strconv.Atoi(os.Args[1])
	for number := 1; number <= max; number++ {
		if first {
			first one
		} else if second {
			second one
		} else if third {
			third one
		} else {
			fourth one
		}
	}
}
